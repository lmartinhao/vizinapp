<div class="wrapper">
  <div class= "container">
    <div class="d-flex justify-content-start mt-4">
      <img src="https://cdn-icons-png.flaticon.com/512/3177/3177440.png" alt="profile-logo" class="logo mt-3">
      <h1 class= "title mx-3">Editar perfil</h1>
    </div>

    <div class="d-flex justify-content-center mt-4">
      <div class="card col-6 px-4 py-2 mt-2">
        <div class="form-group p-2">
          <%= simple_form_for :profile do |f| %>
            <%= f.input :user_name %>
            <%= f.input :email %>
            <%= f.input :password %>
            <%= f.button :submit, "Salvar", class: "btn btn-success btn-sm" %>
          <% end %>
          <div class="mt-4">
            <%= link_to "Página Inicial", root_path, class: "btn btn-primary btn-sm" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
