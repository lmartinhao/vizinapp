<div class="card my-3">
  <div class="card-header">
    <%= note.note_category.name %>
  </div>
  <div class="card-body">
    <h5 class="card-title"><%= note.title %></h5>
    <p class="card-text"><%= note.noteDate %></p>
    <p class="card-text"><%= note.note_file.key  %></p>
    <%#=  current_user.avatar.key, crop: :thumb, gravity: :face, class: "avatar-sm dropdown-toggle", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>

    <%= link_to "Mais detalhes", notes_path, class:"btn btn-info btn-sm" %>
  </div>
  <div class="card-footer text-muted">
    Criado há <%= distance_of_time_in_words_to_now(note.created_at) %>
  </div>
</div>
